name: tiktak
description: A TikTok Clone built with Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.0
  camera: ^0.10.5+9
  flutter_cache_manager: ^3.3.1
  http: ^1.2.0
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
